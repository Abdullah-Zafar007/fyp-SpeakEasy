<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Patient Dashboard - Speech Therapy</title>
    <link rel="stylesheet" href="/styles/interface.css" />
  
    <link rel="stylesheet" href="/styles/shared.css" />
    <link rel="stylesheet" href="/styles/navbtn.css">
    <style>
        :root {
            --primary-color: #008080; /* Teal */
            --secondary-color: #e9f5f5; /* Light Teal */
            --text-color: #3b3b3b; /* Dark Grey */
        }
        /* Global Styles */
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0; padding: 0;
            display: flex;
            background-color: var(--secondary-color);
        }
        .container {
            display: flex;
            width: 100%;
        }
      
        /* Content Styles */
        .content {
            margin-left: 250px;
            padding: 20px;
            width: 100%;
            transition: margin-left 0.3s ease;
            background-color: var(--secondary-color);
        }
        .content.shift {
            margin-left: 0;
        }
       
        .cards {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 30px;
        }
        .card {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            width: calc(33.33% - 20px);
            padding: 20px;
            text-align: center;
            cursor: pointer;
            transition: transform 0.3s ease;
        }
        .card:hover {
            transform: translateY(-10px);
        }
        .card h3 {
            color: var(--primary-color);
            font-size: 24px;
            margin-bottom: 10px;
        }
        .card p {
            color: var(--text-color);
        }
        /* Assigned Lessons Section */
        #assigned-lessons-section {
            display: none;
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            max-width: 900px;
            margin: 0 auto 40px;
        }
        #assigned-lessons-section h2 {
            margin-top: 0;
            color: var(--primary-color);
        }
        #assigned-lessons-container > div {
            border: 1px solid #ccc;
            padding: 15px;
            margin: 10px 0;
            border-radius: 8px;
            background: #fafafa;
        }
        /* Toggle button */
        #lessons-toggle {
            background-color: var(--primary-color);
            color: white;
            border: none;
            border-radius: 5px;
            padding: 12px 20px;
            cursor: pointer;
            font-size: 16px;
            margin-bottom: 20px;
        }
        #lessons-toggle:hover {
            background-color: #006666;
        }
       
@media screen and (max-width: 992px) {
    .content {
        margin-left: 0;
        padding: 15px;
    }

    .cards {
        flex-direction: row;
        flex-wrap: wrap;
        justify-content: center;
        gap: 15px;
    }

    .card {
        width: calc(50% - 15px);
        padding: 15px;
    }

    #assigned-lessons-section {
        padding: 15px;
    }
}

/* Mobile Devices (max-width: 576px) */
@media screen and (max-width: 576px) {
    body {
        flex-direction: column;
    }

    .content {
        margin-left: 0;
        padding: 10px;
    }

    .cards {
        flex-direction: column;
        align-items: center;
        gap: 10px;
    }

    .card {
        width: 100%;
        padding: 15px;
    }

    .card h3 {
        font-size: 20px;
    }

    .card p {
        font-size: 14px;
    }

    #lessons-toggle {
        width: 100%;
        padding: 10px;
        font-size: 15px;
    }

    #assigned-lessons-section {
        padding: 10px;
        margin: 0 5px 30px 5px;
    }
}

    </style>
</head>
<body>
 <%- include('partials/header') %>




    <div class="container">

        <main class="content" id="content">
    <section class="header">

        <h2>Welcome</h2>
        <h3>Journey toward better communication starts here</h3>
        <p>Access your lessons, track your progress, take quizzes, and book appointments.</p>
    </section>

    <button id="lessons-toggle">View Assigned Lessons</button>
        <%- include('partials/navbtn') %>

    <section class="cards">
        <div class="card" id="lessons-card">
            <h3>Lessons</h3>
            <p>Explore interactive lessons to improve your Urdu pronunciation, from basics to advanced.</p>
        </div>
        <div class="card" id="games-card">
            <h3>Games</h3>
            <p>Engage in fun speech therapy games designed to enhance your pronunciation skills.</p>
        </div>
        <div class="card" id="quiz-card">
            <h3>Attempt Quiz</h3>
            <p>Take quizzes to test your Urdu pronunciation accuracy and receive detailed feedback.</p>
        </div>
        <div class="card" id="Practice-card">
            <h3>Lets Practice</h3>
            <p>Click Any Button You want to Practice About.</p>
        </div>
        <div class="card" id="therapist-card">
            <h3>Therapists</h3>
            <p>Book or view your therapy appointments here.</p>
        </div>
        <div class="card" id="Articulation-card">
            <h3>Articulation</h3>
            <p>Book or view your therapy appointments here.</p>
        </div>
   


    </section>
</main>

    </div>
  <%- include('partials/footer') %>
</body>
 <script>
   
    document.getElementById("lessons-card").addEventListener("click", () => window.location.href = "/lessons");
    document.getElementById("games-card").addEventListener("click", () => window.location.href = "/games");
    document.getElementById("quiz-card").addEventListener("click", () => window.location.href = "/quizform");
   
    document.getElementById("therapist-card").addEventListener("click", () => window.location.href = "/readaddtherapist");
    document.getElementById("Articulation-card").addEventListener("click", () => window.location.href = "/articulation");
    document.getElementById("Practice-card").addEventListener("click", () => window.location.href = "/practice");

   
   
</script>

<% if (patient && patient._id) { %>
<script>
    document.getElementById("lessons-toggle").addEventListener("click", () => {
        window.location.href = "/patient/dashboard/<%= patient._id %>";
    });
</script>
<% } %>

</html>
