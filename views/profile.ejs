<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Therapist Card</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        :root {
            --primary-color: #008080;
            --secondary-color: #e9f5f5;
            --text-color: #3b3b3b;
        }
    </style>
</head>
<body class="bg-[var(--secondary-color)] text-[var(--text-color)] font-[Poppins] min-h-screen flex items-center justify-center">
    <div class="w-full max-w-md bg-white rounded-lg shadow-lg p-6">
        <h2 class="text-2xl font-bold text-[var(--primary-color)] mb-6 text-center">Create Therapist Card</h2>
        <form id="registerForm" method="POST" action="/update/<%= userprofile._id %>" enctype="multipart/form-data" class="space-y-4">
            <!-- Name -->
            <div>
                <label for="regName" class="block text-sm font-medium text-gray-700">Name</label>
                <input type="text" id="regName" name="name" placeholder="Enter name" value="<%= userprofile.name %>" 
                    class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:ring-[var(--primary-color)] focus:border-[var(--primary-color)]">
            </div>
        
            <!-- Email -->
            <div>
                <label for="regEmail" class="block text-sm font-medium text-gray-700">Email</label>
                <input type="email" id="regEmail" name="email" placeholder="Enter email" value="<%= userprofile.email %>"  
                    class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:ring-[var(--primary-color)] focus:border-[var(--primary-color)]">
            </div>
        
            <!-- Image Upload -->
            <div>
                <label for="regImage" class="block text-sm font-medium text-gray-700">Image</label>
                
               <input type="text" name="image" id=""> <img src="/uploads/<%= userprofile.image %>" alt="Therapist Image" class="w-full h-48 object-cover rounded-md mb-2">
                    class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:ring-[var(--primary-color)] focus:border-[var(--primary-color)]">
            </div>
        
           
        
            <!-- Submit Button -->
            <div class="text-center">
                <button type="submit" class="w-full bg-[var(--primary-color)] text-white py-2 px-4 rounded-lg hover:bg-teal-600 transition-all">
                    Save Changes
                </button>
            </div>
        </form>
        
    </div>
</body>
</html>
